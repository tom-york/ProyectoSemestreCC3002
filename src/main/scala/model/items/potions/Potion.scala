package model.items.potions

import model.items.Item

/**
 * Trait for potions, extending the `Item` trait.
 * This trait defines the common behaviors for all potions.
 */
trait Potion extends Item {
  // No additional methods or fields are defined here.
}
